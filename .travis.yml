sudo: required
services:
        - docker

before_install:
        - docker build -t zgchuck/docker-react -f Dockerfile.dev .

script:
        - docker run zgchuck/docker-react npm run test -- --coverage
