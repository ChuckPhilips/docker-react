sudo: required
services:
        - docker

before_install:
        - docker build -t zgchuck/docker-react -f Dockerfile.dev .

script:
        - docker run -e CI=true zgchuck/docker-react npm run test
